/** Renders the intentionally minimal landing page until workspace features are implemented. */
export default function HomePage() {
  return (
    <section className="welcome" aria-labelledby="welcome-title">
      <p className="eyebrow">Team productivity, deliberately secure</p>
      <h1 id="welcome-title">Taskify workspace foundation</h1>
      <p>
        The shared web shell is ready. Project boards, tasks, and notifications arrive in the next
        implementation phases.
      </p>
    </section>
  );
}
